@{
    var integration = ViewData["Integration"] as string;
    var hasIntegration = !string.IsNullOrEmpty(integration);
    var pageTitle = ViewData["Title"] as string ?? "CAEI";
    
    // Determine theme class based on integration
    var themeClass = integration?.ToUpperInvariant() switch
    {
        "CAHFS" => "cahfs-theme gparc-theme",
        "EQUINE" => "equine-theme gpare-theme",
        _ => ""
    };
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="@(Context.Request.PathBase)/" />
    <title>@pageTitle - CAEI</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body class="@themeClass">
    <a href="#main-content" class="visually-hidden-focusable">Skip to main content</a>
    
    @await Html.PartialAsync("_Header")

    <main id="main-content" role="main" class="@(hasIntegration ? "container-fluid py-3 px-4" : "container py-3")" tabindex="-1">
        @RenderBody()
    </main>

    <footer class="caei-footer text-center">
        <div class="container">
            <small>CAHFS Aggie Enterprise Integration</small>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>